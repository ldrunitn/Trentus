const express = require('express');
const utentiController = require(process.cwd()+'/controllers/utenti/utentiController');

const router = express.Router();

// le api (suppongo) vanno qui

module.exports = router;