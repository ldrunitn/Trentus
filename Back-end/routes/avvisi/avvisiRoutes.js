const express = require('express');
const avvisiController = require(process.cwd()+'/controllers/avvisi/avvisiController');

const router = express.Router();

// le api (suppongo) vanno qui

module.exports = router;