const express = require('express');
const segnalazioniController = require(process.cwd()+'/controllers/segnalazioni/segnalazioniController');

const router = express.Router();

// le api (suppongo) vanno qui

module.exports = router;